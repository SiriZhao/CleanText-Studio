# Generated locale pages

`../build_readmes.py` writes the committed language-specific project homepages
at the repository root. It intentionally keeps those pages as normal Markdown
instead of requiring GitHub visitors to render a template. This directory is a
home for future human-reviewed locale overrides; it currently has no runtime
or application dependency.
